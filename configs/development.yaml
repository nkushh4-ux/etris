environment: "development"

runtime:
  device: "cuda"
  workers: 1
  batch_size: 1
  half_precision: true

logging:
  level: "DEBUG"

paths:
  data: "./data"
  evidence: "./data/evidence"
  logs: "./data/logs"

processing:
  max_fps: null
  frame_stride: 1